import unittest
from parameterized import parameterized

# This will be the base class for all tests in the xft project.
# We can add custom assertion helpers (e.g., assertAllClose) or
# setup/teardown logic here later as the project grows.

class XftTestCase(unittest.TestCase):
    pass